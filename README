Container Safety Determination

Container-Safety-Determination(CSD) provides a scalable way to classify the safety of the code that runs in containers. 
CSD takes an image and compares all its files with a database of known malicious and non-malicious binaries. 
